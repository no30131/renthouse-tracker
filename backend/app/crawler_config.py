"""
爬蟲搜尋條件設定

這裡調整你想抓的物件條件，不是機密資訊所以不放 .env。
"""

# 縣市 ID 清單（台北=1, 基隆=2, 新北=3, 桃園=6）
REGION_IDS = [1, 2, 3]  # 台北 + 基隆 + 新北

# 行政區 ID（空 list = 整個縣市都抓）
SECTION_IDS: list[int] = []

# 房屋類型："1"=整層住家（排除套房/雅房/分租/共生）
KIND = "1"

# 租金上限（T1 ≤20000、T2 ≤25000，這裡設 T2，前端自行篩 T1）
RENT_MIN = 0
RENT_MAX = 25000

# 最小坪數
AREA_MIN = 30

# 篩選條件
PET = True   # 可養寵物
COOK = True  # 可開伙
HAS_IMAGE = True

# 每次最多抓幾頁（每頁 30 筆）
MAX_PAGES = 5

# 標題關鍵字黑名單（包含任一字串即跳過）
EXCLUDE_KEYWORDS: list[str] = ["共生", "共享", "分租", "雅房", "套房"]

# 排程 Cron（台北時間）
# 格式：分 時 日 月 週
CRON = "0 8 * * 1,3,5"  # 一三五早上 8 點
